# Local dev overrides (loaded when APP_ENV=dev). Do not reuse TICKET_AI_API_URL here — that name is reserved
# for the Python ticket AI base (see .env Orion ecosystem); Java and Symfony both align on that.
TICKET_AI_API_KEY=""
OLLAMA_TICKET_API_URL="http://127.0.0.1:11434/api/chat"
OLLAMA_TICKET_MODEL="qwen2.5:7b"
